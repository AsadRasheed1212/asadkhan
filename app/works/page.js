import WorksClient from './WorksClient';

export const metadata = {
  title: 'Works',
  description: 'Selected projects — Laravel apps, WordPress sites, dashboards, and more.',
};

export default function Page() {
  return <WorksClient />;
}
