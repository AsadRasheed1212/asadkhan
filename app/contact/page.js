import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact',
  description: 'Get in touch for Laravel, WordPress, data entry, or video editing work.',
};

export default function Page() {
  return <ContactClient />;
}
